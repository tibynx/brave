name: "Build Docker image - stable"
on:
  schedule:
    - cron: "10 0 1 * *"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  get_date:
    name: Get current date in RFC 3339
    runs-on: ubuntu-latest
    outputs:
      IMG_DATE: ${{ steps.date.outputs.IMG_DATE }}
    steps:
      - name: Get current date in RFC 3339
        id: date
        run: |
          IMG_DATE=$(date --rfc-3339=seconds | sed 's/ /T/')
          echo "IMG_DATE=${IMG_DATE}" >> $GITHUB_OUTPUT
          echo "The date is $IMG_DATE"

  get_pkg_version:
    name: Get latest package version
    runs-on: ubuntu-latest
    outputs:
      PKG_VER: ${{ steps.version.outputs.PKG_VER }}
    steps:
      - name: Add Brave's APT repo for stable release
        run: |
          sudo curl -fsSLo \
            /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main" | \
            sudo tee -a /etc/apt/sources.list.d/brave-browser-release.list
          sudo apt-get update -y

      - name: Get package version
        id: version
        run: |
          PKG_VER=$(apt list brave-browser | awk -F'[ /]' '/brave-browser/ {print $3}')
          echo "PKG_VER=${PKG_VER}" >> $GITHUB_OUTPUT
          echo "The package version is $PKG_VER"

  build_push:
    name: Build and push image
    runs-on: ubuntu-latest
    needs: [get_date, get_pkg_version]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: 'main'

      - name: Login to GitHub container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image for amd64/arm64
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          file: ./Dockerfile
          push: true
          build-args: |
            IMAGE_BUILD_DATE=${{ needs.get_date.outputs.IMG_DATE }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/brave:latest
            ghcr.io/${{ github.repository_owner }}/brave:stable
            ghcr.io/${{ github.repository_owner }}/brave:${{ needs.get_pkg_version.outputs.PKG_VER }}
            ${{ vars.DOCKERHUB_USERNAME }}/brave:latest
            ${{ vars.DOCKERHUB_USERNAME }}/brave:stable
            ${{ vars.DOCKERHUB_USERNAME }}/brave:${{ needs.get_pkg_version.outputs.PKG_VER }}